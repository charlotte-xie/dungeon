# Copy this file to `.env.local` and fill in your key.
# `.env.local` is gitignored (see .gitignore: *.local).
# No VITE_ prefix — this stays server-side, never bundled into the browser.

XAI_API_KEY=xai-your-key-here

# Optional overrides
# XAI_MODEL=grok-4
# XAI_BASE_URL=https://api.x.ai/v1
